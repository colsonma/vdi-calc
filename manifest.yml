---
applications:
  .:
    name: vdi-calc
    framework:
      name: sinatra
      info:
        mem: 128M
        description: Sinatra Application
        exec: ruby vdi-calc.rb
    infra: aws
    url: ${name}.${target-base}
    mem: 128M
    instances: 1
